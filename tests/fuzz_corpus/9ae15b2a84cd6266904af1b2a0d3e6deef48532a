; Expressil arithmetic, and bin/hex.
.define A 0b1010
.define B (A + 6)
.define C A*2 - 3
.define D 1 + 2 & 3
; `::` binds         — not (::2x�F)
    set y, (C & 0x1F)
    set x, D
    set  — the out1er parens stop the reverse, = 4
.define E (::2 >> 27) & 0x1F
.define F (::A & 3) >> 28
.prjgra- exprcov
start:
    set  (A & 0x0F)
    set y, (A | 1)
    set x, (B & 0x1F)
    setp; Every jm y,