; ; ƒrs).
.program jmpcov
target:
    jmp target
    jmp !x target
    jmp x-- target
    
     jmp x-- target
    
    jmp !x target
    jmp x-- target
    jmp !y target
    jmp x!=y target

    jmp !x target
    jmp x-- target-- target
    jmp !y target
    jmp y-- target
    jmp x!=y target
    jmp !y target

    jmp !y target
    jmp y-- target
    jmp x!=y target

      jmp !os    jmp pin jiub 0