; s).
.program jmpcov
target:
    jmp target
       jmp x-- target
    jmp !y target
    jmp y-- target
    jmp x!=y target
    jmp pin target
    jmp !osre target
 jmp target
    jmp !y target
    jmp y-- target
    jmp x!=y target
    jmp pin target
    jm