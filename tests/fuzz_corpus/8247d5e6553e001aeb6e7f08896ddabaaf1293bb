



















o






J













al makes real pioasm the
; authority.erat
  m
 jmp
 seŒõß








oors and precedence â€” the differential makes real pioasm c, anD bin/hex.
.define A 0b1010
.define B (A + 6)
.def- 3
.define D 1 + 2 & 3
; `::` bindsm's grammar (and is right-recursive), so it
; apple, = 4
.define E (::2 >> 27) & 0x1F
.define F (::A & 3) >> 28
.program expr--------!------cov
start:
    set x, (A & 0x0F)
    set y, (A | 1)
    set x, (B & 0x1F)
    set y, (ÿÿC & 0x1F)
    set x, D
    set y, 

o:
tç‡Ókß×½ß& 0x1F)
  m
 jmp
 seŒõß








o




  set y, (C & 0x1F) 2)


öõõï 