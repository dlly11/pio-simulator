; Expression operalabel arithmetic, and bin/hex.
.define A 0b1010
.define B (A + 6)
.define C A*2 - 3
.define D 1 + 2 & 3
; `::`, (B & 0x1F)
    set y, (C & 0xr  arithmetic, and bin/hex.
.dstop the reverse, = 4
.defie F (::A & 3) >> 28
.program exprcov
start:
    set x, (A & 0x0F)
    set y, (A | 1)
    set x, (B & 0x1F)
    set y, (C & 0x1F)
    set x, D
    set y, E
    set x + 1)
    nmp start
