



















o






J
























=
; Expression opercedence â€” the differential makes real pioasm the
; authority. `.define` valueal makes real pioasm c, and bin/hex.
.define A 0b1010
.define B (A + 6)
.define C A*2 - 3
.define D 1 + 2 & 3
; `::` binds loosest of all in pioasm's grammar (and is right-recursive), so it
; apple, = 4
.define E (::2 >> 27) & 0x1F
.define F (::A & 3) >> 28
.program exprcov
start:
    set x, (A & 0x0F)
    set y, (A | 1)
    set x, (B & 0x1F)
    set y, (C & 0x1F)
    set x, D
    set y, 

o:
tç‡Ókß×½ß& 0x1F)
  m
 jmp
 seŒõß








o


öõßØset y, (C & 0x1F) 2)


öõõï 