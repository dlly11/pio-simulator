; s).
.program jmpcov
target:
    jmp target
    jmp !x target
    jmp x-- target
  jmp !osre target
get:
    jmp target
    jmp !x target
    jmp x-- target
    jmp !y target
     jmp pin target
    jmp !osre target
 mÏ   jm